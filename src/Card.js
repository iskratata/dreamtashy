import React from 'react';

export default function card(props) {
    return (
        <div className='card' style={{backgroundImage: props.bg, backgroundSize: 'cover'}}>
            
        </div>
    );
}